<% if @feed_items.any? %>
  <ol class="microposts">
  <!-- auto call _micropost.html.erb because each element of @feed_items has class Micropost. -->
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
