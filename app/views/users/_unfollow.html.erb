<!-- return a active_relationships. Rails auto uses Ajax with remote: true -->
<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
  html: {method: :delete}, remote: true) do |f| %>
  <%= f.submit t("user.unfollow.unfollow"), class: "btn" %>
<% end %>
